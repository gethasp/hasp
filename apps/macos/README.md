# apps/macos

This directory is reserved for the native Swift macOS application.

Expected future contents:

- Xcode project/workspace
- native app sources
- app-local build helpers

The macOS app is V2 in the current product plan.
